# AIにきく用の指示書

## Inst-Num-01
- このプロジェクトはpython+flaskによる簡単なメモアプリです。現在はローカル開発環境と本番環境のデプロイ先はmodalにて運用をしています。
- まずはこのプロジェクト内のすべてのファイルとディレクトリを読み込んで、しっかりとどういった哲学および設計になっているか理解し、かつ、このメモアプリを作成した作者の意図等もくみとってください。
- その上で、このメモアプリをtypescriptによる他のフロントエンド＋バックエンドライブラリに移植したいと考えています。その場合、以下の3つのパターンを考えてますが、どれが適切だと思いますか？ それぞれのパータンのメリットとデメリット等も併せて教えてください。
1. Astro+Astro標準組み込みのSSRで実装
2. Astro+Islands(Svelte)+SSR
3. Svelte+Sveltekit+SSR

- まずは、ローカル開発環境でのテストランを行いたいので、どのプラットフォームにデプロイするか等の分析は行わなくて結構です（本番環境運用のこと）。また、今回はどのように移植するのかを質問しているだけなので、実際のコード作成は行わないでください。

## Inst-Num-02
- Svelte+Sveltekit+SSRによる実装に移植します。まずは、ローカル環境でのテストを行うので、そこまでのコード作成を以下の数点を考慮しながらお願いします。なおローカルテスト後の本番デプロイ先はcloudflare workers+D1データベースを考えていますので、ローカル・本番の分岐や判断コードが必要な場所があったら、そこは考慮してコードに組み込んでください。
1. データベースはsqlite+drizzle ormを使用する
2. コンポーネントファイルは`src/lib/components`にいれる
3. データベース関連ファイルは、`src/lib/server/db/`に入れ、スキーマ定義ファイルは、`src/lib/server/db/schema`に、マイグレーション関連ファイルは、`src/lib/server/db/migrations`にいれてください。
4. 全体で利用するようなCSSファイルを`src/styles/global.css`に作成し、細かな調整的CSSは`src/routes`内の各+page.svelte内のstyleセクションに書いてください。
5. +layout.svelteや+layout.server.tsが必要な場合は作ってください。
6. drizzle設定ファイルは、`drizzle.config.ts`内に書いてください。
7. sveltekit標準である、viteを利用してください。パッケージマネージャはbunを使ってください。